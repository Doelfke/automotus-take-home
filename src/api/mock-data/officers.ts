import type { Officer } from "../../types";

// Current officer (simulating logged-in user)
export const currentOfficer: Officer = {
  id: "officer-001",
  name: "Officer Martinez",
  badgeNumber: "PE-2847",
};
